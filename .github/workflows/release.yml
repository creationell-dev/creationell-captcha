name: Build WordPress Plugin ZIP, Generate Plugin Manifest & Deploy

on:
  release:
    types: [published]

permissions:
  contents: write   # Release-Assets hochladen + gh-pages-Branch pushen

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      # 2. Setup variables
      - name: Setup variables
        id: vars
        run: |
          echo "PLUGIN_SLUG=$(basename $GITHUB_REPOSITORY)" >> $GITHUB_ENV
          TAG_NAME=${GITHUB_REF##*/}
          TAG_NAME=${TAG_NAME#v}
          echo "TAG_NAME=$TAG_NAME" >> $GITHUB_ENV
          echo "DATE=$(date +'%Y-%m-%d')" >> $GITHUB_ENV
          echo "ZIP_FILE=$(basename $GITHUB_REPOSITORY).zip" >> $GITHUB_ENV
          echo "DOWNLOAD_URL=https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF##*/}/$(basename $GITHUB_REPOSITORY).zip" >> $GITHUB_ENV

      # 3. Setup PHP
      - name: Setup PHP
        uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc # 2.37.1
        with:
          php-version: '8.3'
          tools: composer
          coverage: none

      # 3.1. Install Composer dependencies (runs Strauss via post-install-cmd)
      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist

      # 4. Setup Python
      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.x"

      # 5. Install Pandoc and GraphViz
      #
      # Bewusst ohne Versions-Pin: die Pakete kommen aus den signierten
      # Ubuntu-Quellen des Runner-Images (apt prüft Repo-Signatur und Hash je
      # Paket), und ein fester Pin bricht, sobald GitHub das Image dreht.
      #
      # Anders als phpDocumentor (Schritt 10, läuft NACH ZIP und Prüfsumme)
      # laufen pandoc/jq in Schritt 6, also VOR dem Packen — ein manipuliertes
      # Paket könnte dort in den Workspace schreiben, den Schritt 7 einpackt.
      # Das Restrisiko ist bewusst akzeptiert und hängt an der apt-Signatur-
      # kette; wer es weiter senken will, müsste die .deb-Dateien selbst mit
      # Hash pinnen.
      - name: Install Pandoc and GraphViz
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc jq graphviz

      # 6. Extract metadata and generate HTML
      - name: Extract metadata and generate HTML
        run: |
          mkdir -p gh-pages-html gh-pages-json gh-pages-deploy

          # Plain HTML readme for WordPress backend
          if [ -f README.md ]; then
            pandoc README.md -f markdown -t html -o gh-pages-html/readme.html
          fi

          # Stylesheet der Doku-Website: vendoriert unter .github/simple.min.css
          # (von build-public-staging.sh aus .github/public-repo/simple.min.css
          # dorthin kopiert) statt zur Build-Zeit von cdnjs geladen — W3-10/A-3:
          # ein CDN-Ausfall oder eine zurückgezogene Version durfte vorher das
          # gesamte Release kippen (kein ZIP, kein Manifest, kein gh-pages-
          # Deploy), wegen eines reinen Doku-Stylesheets, das den Build vorher
          # nie berühren konnte (`<link>`-Tag zur Laufzeit). git selbst sichert
          # jetzt die Integrität der Datei; zum Aktualisieren siehe
          # DEVELOPMENT.md, Abschnitt 7.
          cp .github/simple.min.css gh-pages-html/simple.min.css

          # Original styled HTML für gh-pages Webseite (optional)
          pandoc README.md -f markdown -t html5 \
            -s -o gh-pages-html/index.html \
            --metadata title="Plugin Documentation" \
            --css simple.min.css \
            --metadata pagetitle="CreaCaptcha Plugin Docs"

          # Extract sections
          pandoc -f markdown -t html <(awk '/^## Description/{flag=1;next}/^## /{flag=0}flag' README.md) -o gh-pages-json/description.html || true
          pandoc -f markdown -t html <(awk '/^## Installation/{flag=1;next}/^## /{flag=0}flag' README.md) -o gh-pages-json/installation.html || true
          pandoc -f markdown -t html <(awk '/^## Changelog/{flag=1;next}/^## /{flag=0}flag' README.md) -o gh-pages-json/changelog.html || true
          pandoc -f markdown -t html <(awk '/^## FAQ/{flag=1;next}/^## /{flag=0}flag' README.md) -o gh-pages-json/faq.html || true

          # Extract metadata for JSON
          declare -A META
          while IFS=":" read -r key value; do
            key=$(echo "$key" | sed -E 's/^\*\*//;s/\*\*$//;s/[[:space:]]//g')
            # W3-11a: ein literales Tab MITTEN im Wert (z. B. "**Tested up to:**
            # 6.9<TAB>(beta)") wuerde weiter unten von split("\t") als zweiter
            # Trenner gelesen und den Teil danach still verwerfen — keine
            # Injection (das war LK-15 und ist gefixt), aber eine stille
            # Datenverkuerzung. Ein Tab traegt in einer README-Metazeile ohnehin
            # keine Bedeutung, deshalb wird er hier durch ein Leerzeichen
            # ersetzt, BEVOR er den Tab-getrennten Zwischenschritt erreicht.
            value=$(echo "$value" | sed -E 's/^\*\*//;s/\*\*$//;s/^[[:space:]]*//;s/[[:space:]]*$//;s/\t/ /g')
            [ -n "$key" ] && META[$key]="$value"
          done < <(grep -E '^\*\*[^:]+:\*\*' README.md)

          # JSON-Erzeugung über jq statt printf: ein Anführungszeichen oder
          # Backslash in einer `**Key:**`-Zeile der README brach sonst entweder
          # das JSON oder schleuste einen zusätzlichen Manifest-Schlüssel ein
          # (`**Version:** 1.0.2", "Testedupto": "9.9` reichte). jq kodiert
          # Schlüssel und Werte korrekt; Tab trennt beide, weil er in den
          # Metazeilen nicht vorkommt — der obige `s/\t/ /g` stellt das jetzt
          # auch für einen Wert mit einem eingebetteten Tab sicher.
          {
            for k in "${!META[@]}"; do
              printf '%s\t%s\n' "$k" "${META[$k]}"
            done
          } | jq -R -s '
                split("\n")
                | map(select(length > 0) | split("\t") | {(.[0]): (.[1] // "")})
                | add // {}
                | . + {"_generated": true}
              ' > gh-pages-json/readme_meta.json

          # Contributors & Tags — ebenfalls über jq, damit die Werte kodiert
          # werden. Die Zuweisung an $GITHUB_ENV nutzt zusätzlich die
          # Delimiter-Form: ein Zeilenumbruch im Wert könnte sonst weitere
          # Umgebungsvariablen für alle folgenden Schritte setzen.
          CONTRIBUTORS=$(grep -m1 -E '^\*\*Contributors:\*\*' README.md \
            | sed -E 's/^\*\*Contributors:\*\*[[:space:]]*//' \
            | jq -R -c 'split(",") | map(gsub("^[[:space:]]+|[[:space:]]+$";"")) | map(select(length > 0))')
          TAGS=$(grep -m1 -E '^\*\*Tags:\*\*' README.md \
            | sed -E 's/^\*\*Tags:\*\*[[:space:]]*//' \
            | jq -R -c 'split(",") | map(gsub("^[[:space:]]+|[[:space:]]+$";"")) | map(select(length > 0))')
          {
            echo "CONTRIBUTORS<<CC_MANIFEST_EOF"
            echo "$CONTRIBUTORS"
            echo "CC_MANIFEST_EOF"
            echo "TAGS<<CC_MANIFEST_EOF"
            echo "$TAGS"
            echo "CC_MANIFEST_EOF"
          } >> "$GITHUB_ENV"

          # Copy README.md to gh-pages for reference
          cp README.md gh-pages-deploy/

      # 7. Create plugin ZIP
      - name: Create plugin ZIP
        run: |
          STAGING_DIR="${{ env.PLUGIN_SLUG }}"
          mkdir -p "$STAGING_DIR"
          rsync -a \
            --exclude='.git' \
            --exclude='.gitignore' \
            --exclude='.github' \
            --exclude='.DS_Store' \
            --exclude="$STAGING_DIR" \
            --exclude='*.zip' \
            --exclude='*.sha256' \
            --exclude='gh-pages-*' \
            --exclude='docs' \
            --exclude='phpdoc.xml' \
            --exclude='phpDocumentor.phar' \
            --exclude='node_modules' \
            --exclude='vendor' \
            --exclude='composer.json' \
            --exclude='composer.lock' \
            . "$STAGING_DIR/"
          zip -r "${{ env.ZIP_FILE }}" "$STAGING_DIR"
          rm -rf "$STAGING_DIR"

      # 7.1. Generate SHA256 checksum for security verification
      #
      # Artefakt und Prüfsumme entstehen bewusst im selben Job: Die Prüfsumme
      # sichert die Strecke Build → Nutzer ab (das ZIP liegt auf github.com,
      # das Manifest mit der Prüfsumme auf github.io — zwei Hosts), nicht die
      # Integrität des Builds gegen sich selbst. Wer diesen Job manipulieren
      # kann, kann beides ändern; dagegen hilft nur eine unabhängige
      # Attestierung, kein zweiter Schritt im selben Job. Zugriff auf den Job
      # setzt Maintainer-Rechte am Repo voraus.
      - name: Generate SHA256 checksum
        run: |
          SHA256_HASH=$(sha256sum "${{ env.ZIP_FILE }}" | awk '{print $1}')
          echo "SHA256_HASH=$SHA256_HASH" >> $GITHUB_ENV
          echo "SHA256 Hash: $SHA256_HASH"
          # Create checksum file for reference
          echo "$SHA256_HASH  ${{ env.ZIP_FILE }}" > ${{ env.ZIP_FILE }}.sha256

      # 8. Upload ZIP and checksum to GitHub release
      - name: Upload ZIP and checksum to GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          files: |
            ${{ env.ZIP_FILE }}
            ${{ env.ZIP_FILE }}.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 9. Generate JSON manifest dynamically
      - name: Generate plugin manifest JSON
        shell: python
        run: |
          import os, json

          def load_section_html(name):
              path = f"gh-pages-json/{name}.html"
              return open(path, encoding="utf-8").read() if os.path.exists(path) else ""

          meta = {}
          meta_path = "gh-pages-json/readme_meta.json"
          if os.path.exists(meta_path):
              with open(meta_path, encoding="utf-8") as f:
                  meta = json.load(f)

          raw = os.environ.get("CONTRIBUTORS", '["creationell-dev"]')
          contributors_raw = json.loads(raw) if raw.strip() else ["creationell-dev"]
          contributors = {}
          for username in contributors_raw:
              username = username.strip()
              if username:
                  contributors[username] = {
                      "display_name": username,
                      "profile": f"https://profiles.wordpress.org/{username}",
                      "avatar": f"https://wordpress.org/grav-redirect.php?user={username}&s=36"
                  }

          tags_raw = os.environ.get("TAGS", '["captcha","spam","anti-spam","anti-bot","proof of work"]')
          tags = json.loads(tags_raw) if tags_raw.strip() else ["captcha","spam","anti-spam","anti-bot","proof of work"]

          def clean_meta_value(value):
              if isinstance(value, str):
                  return value.replace("**", "").replace("*", "").strip()
              return value

          plugin = {
              "name": clean_meta_value(meta.get("PluginName", "")),
              "display_name": clean_meta_value(meta.get("PluginName", "")),
              "slug": os.environ.get("PLUGIN_SLUG"),
              "version": clean_meta_value(meta.get("Version", os.environ.get("TAG_NAME"))),
              "download_url": os.environ.get("DOWNLOAD_URL"),
              "checksum_sha256": os.environ.get("SHA256_HASH", ""),
              "requires": clean_meta_value(meta.get("Requiresatleast", "6.9")),
              "tested": clean_meta_value(meta.get("Testedupto", "6.9")),
              "requires_php": clean_meta_value(meta.get("RequiresPHP", "8.3")),
              "author": clean_meta_value(meta.get("Author", "")),
              "author_profile": clean_meta_value(meta.get("AuthorURI", "")),
              "contributors": contributors,
              "tags": [t.strip() for t in tags],
              "license": clean_meta_value(meta.get("License", "GPL-2.0+")),
              "license_uri": clean_meta_value(meta.get("LicenseURI", "http://www.gnu.org/licenses/gpl-2.0.txt")),
              "text_domain": clean_meta_value(meta.get("TextDomain", "")),
              "domain_path": clean_meta_value(meta.get("DomainPath", "/languages")),
              "network": str(meta.get("Network", "false")).lower() == "true",
              "requires_plugins": [clean_meta_value(p.strip()) for p in meta.get("RequiresPlugins", "").split(",") if p.strip()],
              "homepage": clean_meta_value(meta.get("PluginURI", f"https://github.com/creationell-dev/{os.environ.get('PLUGIN_SLUG')}")),
              "last_updated": os.environ.get("DATE"),
              "sections": {
                  "description": load_section_html("description"),
                  "installation": load_section_html("installation"),
                  "changelog": load_section_html("changelog"),
                  "faq": load_section_html("faq"),
              },
              "readme_html": open("gh-pages-html/readme.html", encoding="utf-8").read() if os.path.exists("gh-pages-html/readme.html") else "",
              "banners": {
                  "low": f"https://{os.environ.get('GITHUB_REPOSITORY').split('/')[0].lower()}.github.io/{os.environ.get('PLUGIN_SLUG')}/assets/banner-772x250.avif",
                  "high": f"https://{os.environ.get('GITHUB_REPOSITORY').split('/')[0].lower()}.github.io/{os.environ.get('PLUGIN_SLUG')}/assets/banner-1544x500.avif"
              },
              "icons": {
                  "default": f"https://s.w.org/plugins/geopattern-icon/{os.environ.get('PLUGIN_SLUG')}.svg"
              }
          }

          os.makedirs("gh-pages-json", exist_ok=True)
          with open(f"gh-pages-json/plugin_{os.environ.get('PLUGIN_SLUG')}.json", "w", encoding="utf-8") as f:
              json.dump(plugin, f, indent=2, ensure_ascii=False)

      # 10. Generate PHPDoc API documentation (gepinnt auf v3.9.1 + Prüfsumme)
      #
      # Ein Release-Tag auf GitHub lässt sich umhängen und ein Release-Asset
      # ersetzen; der phar wird hier ausgeführt. Deshalb Version UND SHA-256
      # gepinnt (Wert = der von der GitHub-API gemeldete Asset-Digest für
      # v3.9.1). Beim Anheben der Version die Prüfsumme miterneuern —
      # Verfahren in DEVELOPMENT.md, Abschnitt 7.
      - name: Generate PHPDoc API documentation
        run: |
          PHPDOC_VERSION=v3.9.1
          PHPDOC_SHA256=ef5509af790c8e56d11fff8162f12c7c19473b9af6e79b0fb9a62aff26da2ea5
          wget -q "https://github.com/phpDocumentor/phpDocumentor/releases/download/${PHPDOC_VERSION}/phpDocumentor.phar" -O phpDocumentor.phar
          echo "$PHPDOC_SHA256  phpDocumentor.phar" | sha256sum -c -
          chmod +x phpDocumentor.phar
          ./phpDocumentor.phar run --config=phpdoc.xml
          echo "PHPDoc documentation generated in docs/"

      # 11. Prepare for deployment
      - name: Prepare gh-pages deployment
        run: |
          mkdir -p gh-pages-deploy
          cp -r gh-pages-json/* gh-pages-deploy/
          cp -r gh-pages-html/* gh-pages-deploy/
          if [ -d "docs" ]; then
            cp -r docs gh-pages-deploy/
          fi
          if [ -d "assets" ]; then
            cp -r assets gh-pages-deploy/
          fi

      # 12. Deploy to GitHub Pages
      #
      # keep_files: false ist Absicht — der gh-pages-Branch ist reines
      # Build-Ergebnis und wird bei jedem Release vollständig ersetzt. Damit
      # bleiben keine Doku-Dateien aus früheren Versionen liegen, und das
      # Update-Manifest beschreibt immer genau den aktuellen Stand. Der Preis:
      # ein Re-Run für einen älteren Tag würde das Manifest auf diese ältere
      # Version zurücksetzen (Re-Sync deshalb nur für den neuesten Tag).
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@84c30a85c19949d7eee79c4ff27748b70285e453 # v4.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: gh-pages-deploy
          publish_branch: gh-pages
          keep_files: false
